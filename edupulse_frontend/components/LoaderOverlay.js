import FullScreenLoader from "./FullScreenLoader";

/** @deprecated Use FullScreenLoader — same as route loading in _app.js */
export default function LoaderOverlay() {
  return <FullScreenLoader />;
}
